{
  "name": "ai-career-copilot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "cd backend && ..\\.venv\\Scripts\\python.exe -m uvicorn app:app --reload --host 127.0.0.1 --port 8000",
    "frontend": "cd frontend && npm start",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}